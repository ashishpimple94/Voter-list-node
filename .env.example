# MongoDB Connection
# Replace with your MongoDB Atlas connection string or local MongoDB URI
MONGODB_URI=mongodb://localhost:27017/excel-voter-data
# For MongoDB Atlas, use format:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database-name?retryWrites=true&w=majority

# Server Port (Render will set this automatically, but you can specify a default)
PORT=8000

# Environment Mode
NODE_ENV=production
